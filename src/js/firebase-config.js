// Firebase client config — populated from console.firebase.google.com → Project Settings → General → Your apps.
// Only Hosting is used in week 1. Firestore/Analytics wired up in later weeks.
// Note: apiKey is a public identifier for Firebase Web — not a secret. Real access is gated by security rules.

window.firebaseConfig = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME.firebaseapp.com',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME.appspot.com',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME'
};
